const Header = () => {

    return <header>
        <select>
            <option value="default">Select</option>
            <option value="date">Date</option>
        </select>
    </header>
}

export default Header